---
---

<section class="offer-hero" aria-labelledby="offer-hero-title">
  <div class="offer-hero__overlay"></div>
  <div class="offer-hero__content">
    <h1 id="offer-hero-title">Ponuka</h1>
  </div>
</section>

<style>
  .offer-hero {
    position: relative;
    min-height: 38vh;
    display: grid;
    place-items: center;
    color: #ffffff;
    background: url('/images/banners/offer-banner.jpg') center/cover no-repeat;
  }
  .offer-hero__overlay {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.35);
  }
  .offer-hero__content {
    position: relative;
    z-index: 1;
    text-align: center;
    padding: 3rem 1rem;
  }
  .offer-hero h1 {
    margin: 0;
    font-size: clamp(1.75rem, 3.5vw, 3rem);
    letter-spacing: 0.01em;
  }
</style>
