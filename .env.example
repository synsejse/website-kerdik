DB_DATABASE=messages
DB_USER=appuser
DB_PASSWORD=apppassword
DB_ROOT_PASSWORD=approotpassword

ADMIN_PASSWORD_HASH= # Generate with: cargo run --manifest-path bcrypt-gen/Cargo.toml -- <password>

# Optional Configuration
ROCKET_PORT=8080
ROCKET_ADDRESS=0.0.0.0
STATIC_DIR=/app/static
