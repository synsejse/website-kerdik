DB_DATABASE=messages
DB_USER=appuser
DB_PASSWORD=apppassword
DB_ROOT_PASSWORD=approotpassword

# App Configuration
# DATABASE_URL is constructed automatically in docker-compose,
# but you can set it manually for local development:
# DATABASE_URL=mysql://appuser:apppassword@localhost:3306/messages

ADMIN_PASSWORD_HASH= # Generate with: cargo run --bin hash_password

# Optional Configuration
ROCKET_PORT=8080
ROCKET_ADDRESS=0.0.0.0
STATIC_DIR=/app/static
